# database_url = "postgresql+psycopg://..."

[llm]
provider = "gemini"
model = "gemini-3.1-flash-lite"

# [ollama]
# base_url = "http://host.docker.internal:11434"

# [anthropic]
# api_key = "sk-ant-..."  # defaults to $ANTHROPIC_API_KEY

# [gemini]
# api_key = "AIza..."     # defaults to $GEMINI_API_KEY

# [notify.discord]
# webhook_url = "https://discord.com/api/webhooks/CHANGE-ME"

[schedule]
interval_minutes = 60

[web]
base_url = "http://localhost:8000"  # used for the "review all" link in notifications
